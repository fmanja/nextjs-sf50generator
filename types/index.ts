// Types for AI Assistant standalone app
export interface Employee {
  id: string;
  name: string;
  series: number; // e.g., 0201
  grade: number; // 5-15
  step: number; // 1-10
  salary: number; // dollars
  dutyStation: string; // city, state
}

